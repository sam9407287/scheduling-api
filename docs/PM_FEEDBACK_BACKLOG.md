# PM 回饋待辦清單（2026-09-18 雨蓉Jessica 實測）

> 根因分析完成日 2026-09-18；狀態欄由 Sam 更新。

| # | 回饋 | 根因歸屬 | 修法 | 工作量 | 狀態 |
|---|---|---|---|---|---|
| 1 | 沒有「排休」設定（診所固定週日公休） | 設計缺口（前後端各半）。員工層級每週 blocked 時段已存在且 solver 支援；缺「機構層級公休日」概念 | 後端加機構每週公休日設定＋合規/solver 接入；前端設定 UI＋班表灰底 | 後端半天＋前端 | 待辦 |
| 2 | 排錯班無法改版本名稱 | 純前端缺 UI。後端 `PATCH /versions/{id}` 的 `version_label` 本來就可寫 | 前端加重新命名入口 | 很小 | 待辦（前端） |
| 3 | 無法新增機構 | 按 MVP 設計（Google 登入自動開機構）。後端 OrganizationViewSet 允許 manager POST 但 User.organization 單值、建了切不過去＝陷阱 | 前端藏「新增機構」入口；後端收成 superuser 限定 | 小 | 待產品拍板 |
| 4 | 簽核總表只能一週一週跳 | 純前端限制。後端 approved-timeline 支援任意 date_from/date_to（≤62天） | 前端加日期選擇器 | 小 | 待辦（前端） |
| 5a | 同一天排不進兩個班（7-1＋2'-10'） | 待前端確認。後端唯一鍵允許同日不同班別、solver 支援多班次 | 前端實測同日兩班操作路徑；可能是該機構未建對應班別模板（教育） | 確認後定 | 待確認 |
| 5b | 兩版本簽核後總表看不到班 | 大概率＝#4 的副作用（班在 6 月、總表停在本週，要按 13 次上一週）。後端 entries-based 挑選已驗證正確 | 先做 #4，之後仍重現再查後端 | 隨 #4 | 待觀察 |

## 補充脈絡
- #2 的情境（10 月版本裡排 6 月的班）後端完全合法——版本期間自動外擴，改名即可自救。
- #5a PM 是照紙本班表試排，班別代號（7-1、2'-10'）是長照分段班慣例，可能需要先在班別管理建模板。
