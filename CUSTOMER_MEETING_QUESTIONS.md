# 客戶會議問題清單 — 排班系統

> 會議日期：2026-05-05
> 文件用途：列出目前後端設計中「我們做了的假設」與「需要客戶拍板」的決策。每一題後面都標註了在程式中已預設的值，方便客戶用「同意 / 不同意 / 需修改」三種方式回答。
> 緊急程度：🔴 高（不釐清會直接做錯）｜🟡 中（會影響使用者體驗）｜🟢 低（可後期調整）

---

## 目錄

1. [勞動法規與工時假設](#1-勞動法規與工時假設) 🔴
2. [組織與權限結構](#2-組織與權限結構) 🔴
3. [員工與合約管理](#3-員工與合約管理) 🟡
4. [班別（ShiftTemplate）設計](#4-班別shifttemplate設計) 🟡
5. [可用性與時段設定](#5-可用性與時段設定) 🟡
6. [AI 排班引擎假設](#6-ai-排班引擎假設) 🔴
7. [加班政策](#7-加班政策) 🔴
8. [出勤與異常處理](#8-出勤與異常處理) 🟡
9. [合規檢查行為](#9-合規檢查行為) 🟡
10. [稽核與資料保留](#10-稽核與資料保留) 🟢
11. [前端整合與 UX](#11-前端整合與-ux) 🟡
12. [部署、認證與整合](#12-部署認證與整合) 🔴
13. [尚未實作但可能需要的功能](#13-尚未實作但可能需要的功能) 🟡

---

## 1. 勞動法規與工時假設

| # | 問題 | 目前預設 | 來源檔案 |
|---|------|---------|---------|
| 1.1 🔴 | 適用哪一個地區的勞動法規？台灣《勞動基準法》、中國《勞動法》、其他？ | 台灣勞基法 | `compliance/engine.py:18-24` |
| 1.2 🔴 | 一週的起始日是星期幾？ | 星期一（ISO 8601） | `compliance/engine.py:154` |
| 1.3 🔴 | 每週最大正常工時上限？ | 40 小時 | `DEFAULT_RULES['max_weekly_hours']` |
| 1.4 🔴 | 兩班之間的最小休息時間？ | 11 小時（勞基法第 34 條） | `DEFAULT_RULES['min_rest_hours']` |
| 1.5 🔴 | 最大連續工作天數？ | 6 天（第 7 天必須休息） | `DEFAULT_RULES['max_consecutive_days']` |
| 1.6 🔴 | 「每週至少 1 天休息」是必檢查（hard rule）還是僅警告（advisory）？目前**僅定義未強制執行**，AI 引擎不會擋。 | 未強制 | `engine.py` 未實作 |
| 1.7 🔴 | 「每日最大工時 8 小時」是否必檢查？目前**僅定義未強制執行**。 | 未強制 | `engine.py` 未實作 |
| 1.8 🟡 | 是否有「每月最大工時」上限（含加班）？目前未實作。 | 無 | — |
| 1.9 🟡 | 是否有「特殊休息日」概念（例如國定假日加倍給薪）？目前 OvertimeType 有 `holiday` / `special_holiday` 兩種，是否需要連動國定假日表？ | 已預留欄位但無國定假日表 | `overtime/models.py:27-32` |
| 1.10 🟡 | 法規規則是否需要可由客戶自行調整（例如「我們自願給 12 小時休息」），或是一律遵循國家法定下限？ | 程式碼中有 `LaborLawRule` 表但**目前未被引擎讀取** | `compliance/models.py` |

---

## 2. 組織與權限結構

| # | 問題 | 目前預設 | 來源 |
|---|------|---------|------|
| 2.1 🔴 | 組織層級結構是「Organization → Branch（兩層）」還是有更深？例如「總公司 → 區域 → 分店 → 部門」 | 兩層（Organization + Branch） | `organizations/models.py` |
| 2.2 🔴 | Branch 在貴公司的真正名稱是什麼？分店、據點、部門、單位、廠區？ | 程式內稱 `Branch`，前端顯示「分店」 | — |
| 2.3 🔴 | 員工是否會「跨 Branch 支援」？是否需要「主要 Branch + 可支援 Branch 清單」？目前 `Attendance` 有 `is_cross_branch` 欄位但 Employee 沒有可支援清單。 | 無可支援清單 | `employees/models.py` |
| 2.4 🔴 | 角色預設只有四種：admin / manager / supervisor / employee。是否符合貴公司編制？例如有沒有「店長」「副店長」「組長」需要區分？ | 四種固定角色 | `accounts/models.py:8-22` |
| 2.5 🔴 | Manager 能不能看到「其他 Branch」的員工排班？目前 manager 受 branch 限制（如果有 branch）。 | 受 branch 限制 | `employees/views.py:65-73` |
| 2.6 🟡 | 員工能不能看到「同事」的排班？還是只能看自己的？目前所有受認證使用者皆可讀取（受 org 範圍）。 | 同 org 全可看 | `schedules/views.py` |
| 2.7 🟡 | 是否需要「審核流程」？例如班表必須由 Supervisor 提交、Manager 核准。目前有 `approve` 端點但沒有「拒絕並退回」流程。 | 只有單向 approve | `schedules/views.py:46-71` |
| 2.8 🟡 | 一個使用者能否同時屬於多個 Organization？目前 User → Organization 是 ForeignKey（單一）。 | 單一 Organization | `accounts/models.py` |

---

## 3. 員工與合約管理

| # | 問題 | 目前預設 | 來源 |
|---|------|---------|------|
| 3.1 🟡 | 合約類型只有三種：full_time / part_time / dispatch（派遣）。是否需要增加：fixed_term（定期）、intern（實習）、contractor（外包）、apprentice（學徒）？ | 三種固定 | `employees/models.py:28-32` |
| 3.2 🟡 | 員工的 `position`（職位）目前是**自由文字**，不限制。是否需要改成「下拉選單 + 客戶可維護」？例如護理師、藥師、行政、清潔。 | 自由文字 | `employees/models.py` |
| 3.3 🟡 | 一位員工能否同時持有多個 Active Contract（例如同時兼差兩個職位）？目前模型支援，但 Employee 主檔同步機制不明確。 | 程式允許但邏輯不明 | `employees/models.py:197-241` |
| 3.4 🟡 | 證照（Certification）是否分組織？目前**全系統共用一份**，例如「護理師執照」全公司共用。 | 全系統共用 | `employees/models.py:9-23` |
| 3.5 🟡 | 證照需求是 AND 還是 OR？例如「需要 CPR **且** 急救」vs「需要 CPR **或** 急救」？目前是 AND（全部需要）。 | AND | `ortools_provider.py:484-494` |
| 3.6 🟡 | 是否需要「證照即將到期提醒」？目前 Certification 有 `expiry_date` 欄位但無提醒機制。 | 有欄位無提醒 | `employees/models.py` |
| 3.7 🟢 | 員工離職要保留多久才能刪除？目前是軟刪除（`is_active=False`），不會真的刪除。 | 軟刪除永久保留 | `employees/models.py` |

---

## 4. 班別（ShiftTemplate）設計

| # | 問題 | 目前預設 | 來源 |
|---|------|---------|------|
| 4.1 🟡 | 一個班別（例如「早班」）是否會有不同變體（例如「早班 A 09:00-17:00」「早班 B 08:00-16:00」）？目前每個 ShiftTemplate 只有單一時段。 | 單一時段 | `shifts/models.py:9-61` |
| 4.2 🟡 | 班別的休息時間（`break_minutes`）預設 0，且是「整段休息」。如果客戶有「兩段休息」（例如午休 60 分 + 下午茶 15 分），如何處理？ | 整段休息 | `ShiftTemplate.break_minutes` |
| 4.3 🟡 | `break_minutes` 是**從 expected_hours 中扣除**（不算工時）還是**包含於工時**（給薪）？目前是**扣除**。 | 扣除（不給薪） | `ShiftTemplate.duration_hours` |
| 4.4 🟡 | `overlap_minutes`（交接時間，預設 30）目前**只記錄不執行**——AI 引擎不會檢查兩班是否有 30 分鐘交班重疊。是否需要實作？ | 預設 30 但未強制 | `shifts/models.py` |
| 4.5 🟡 | 跨午夜班別（例如 22:00–06:00）目前以「end_time < start_time」自動判定為跨日。是否會出現「24 小時班」（連續 24 小時）？目前不支援。 | 跨日支援，24 小時班不支援 | `ortools_provider.py:56` |
| 4.6 🟡 | 一個員工同一天**最多排幾個班**？目前硬限制是 1 班/天（不支援拆班、回頭班、split shift）。 | 1 班/天 | `Schedule unique_together` + `ortools_provider.py:469` |
| 4.7 🟡 | 班別的 `min_staff_count` 是「**至少**幾人」還是「**剛好**幾人」？目前是至少（hard constraint）。是否需要 `max_staff_count`？ | 只有 min | `shifts/models.py` |

---

## 5. 可用性與時段設定

| # | 問題 | 目前預設 | 來源 |
|---|------|---------|------|
| 5.1 🟡 | 每位員工的「不可排班時段（blocked）」與「偏好時段（preferred）」可以「套用到特定星期幾」或「每天」。是否需要支援「特定日期」（例如「2026-05-15 不能排」）？目前該功能在 `unavailable_dates`（AI 引擎參數）支援，但 UI 無對應欄位。 | 模型未直接支援，僅週次循環 | `employees/models.py:141-194` |
| 5.2 🟡 | 「特殊規則文字」（special_rules）是給 LLM 排班用的自然語言提示。**目前 OR-Tools 引擎並不會讀取這個欄位**——僅未來 LLM provider 才會用。客戶是否認知這點？ | 僅 LLM provider 會讀 | `employees/models.py:117-118` |
| 5.3 🟡 | `EmployeeAvailability` 是 OneToOne（一位員工一筆）。如果員工說「3/1-3/15 我每週只能排 20 小時，3/16 之後改回 40 小時」，**目前無法處理**——需要客戶先確認是否需要分時段設定。 | 不支援多筆 | `employees/models.py:93-138` |
| 5.4 🟡 | `effective_from` / `effective_to` 欄位**目前未被 AI 引擎使用**——AI 一律用最新一筆 Availability。這是 bug 還是 by design？ | 模型有但邏輯未用 | `employees/models.py:108-110` |
| 5.5 🟡 | 員工自己能不能編輯「自己的可用性」？還是只有 supervisor 才能改？目前是 IsSupervisor。 | 只有 supervisor | `employees/views.py:154-189` |

---

## 6. AI 排班引擎假設

> 這是整個系統的核心，請特別跟客戶逐條確認。

### 6.1 硬約束（一定遵守，違反就無解）

| # | 約束 | 目前預設 | 是否合理？ |
|---|------|---------|-----------|
| 6.1.1 🔴 | 班別最少人數 | `min_staff_count` 必達 | ☐ |
| 6.1.2 🔴 | 一人一日一班 | 不支援 split shift | ☐ |
| 6.1.3 🔴 | 員工不可排日期 | `unavailable_dates` 內絕不排 | ☐ |
| 6.1.4 🔴 | 證照需求 | 必須持有**全部**列出的證照（AND） | ☐ |
| 6.1.5 🔴 | Blocked 時段 | 與 blocked 時段重疊就絕不排 | ☐ |

### 6.2 軟約束（盡量達成，可違反但會扣分）

| # | 約束 | 預設權重/罰分 | 是否合理？ |
|---|------|------------|-----------|
| 6.2.1 🔴 | 公平性（每位員工排班數差距） | 權重 10 × (max - min) | ☐ |
| 6.2.2 🔴 | 班別偏好分數（員工對班別的喜好 0-10） | 罰分 = max(0, 10 - 偏好分數) | ☐ |
| 6.2.3 🔴 | 偏好時段（preferred 時段） | 不在偏好時段內 = 罰分 3 | ☐ |
| 6.2.4 🔴 | 員工優先順位（ShiftEmployeePriority，新功能） | rank 1=罰分 0, 2=3, 3=6, 4=8, 未列名=10 | ☐ |
| 6.2.5 🔴 | `max_extra_shifts` 加班上限 | 超過 = 罰分 20/班 | ☐ |
| 6.2.6 🔴 | 每週工時達標 | 不足每小時罰 5、超過每小時罰 2 | ☐ |

**請客戶確認：**

- 6.2.7 🔴 上述「公平性 vs 員工偏好 vs 加班意願」的相對權重是否合理？例如目前「公平性」權重 10、員工偏好最多 10、優先順位最多 10。如果客戶覺得「員工偏好應該優先於公平性」，要把公平性權重調低。
- 6.2.8 🔴 ShiftEmployeePriority 的「rank → 罰分」對照表（1→0, 2→3, 3→6, 4→8, 未列→10）是否合理？是否需要動態調整？
- 6.2.9 🟡 「每週工時不足罰 5、超過罰 2」表示**寧可加班也不缺工時**，這是客戶想要的政策嗎？

### 6.3 OR-Tools 計算限制

- 6.3.1 🟡 OR-Tools 求解最長時間 **300 秒**（5 分鐘）。如果規模太大會 timeout。請問客戶單次排班最大規模？（員工數 × 天數 × 班別數）
- 6.3.2 🟡 是否需要「漸進式排班」（先排幾天、看結果、再繼續）？目前是一次性。
- 6.3.3 🟡 是否會有「LLM 自然語言排班」需求？目前僅 OR-Tools provider，但架構允許 plug-in。

---

## 7. 加班政策

| # | 問題 | 目前預設 | 來源 |
|---|------|---------|------|
| 7.1 🔴 | 加班費倍率：regular = **1.34×**, rest_day / holiday / special_holiday = ?（**目前全部預設 1.34，需確認**） | 全為 1.34 | `overtime/models.py:42` |
| 7.2 🔴 | 何時開始計算加班？「超過排班 expected_hours」就算？還是「超過每日 8 小時」才算？**目前未自動判定**——需手動建立 `OvertimeRecord`。 | 手動建立 | `overtime/views.py` |
| 7.3 🔴 | 「OvertimeRecord.calculated_amount」（金額）目前**不會自動計算**，只有手動填入。是否需要實作自動計算（hourly_rate × hours × multiplier）？ | 手動填入 | `overtime/models.py:54` |
| 7.4 🔴 | 「每日加班上限」與「每月加班上限」目前**已定義在 OvertimeRule，但 AI 引擎與合規引擎都沒檢查**。是否需要強制？ | 未強制 | `overtime/models.py:71-115` |
| 7.5 🟡 | 加班是否要區分「自願加班」與「指派加班」？目前無此欄位。 | 無區分 | — |
| 7.6 🟡 | 是否會發生「補休（換假）」？目前無此模型。 | 不支援 | — |

---

## 8. 出勤與異常處理

| # | 問題 | 目前預設 | 來源 |
|---|------|---------|------|
| 8.1 🟡 | 打卡寬限期 5 分鐘（5 分內不算遲到），是否合理？ | 5 分鐘 | `attendance/views.py:139` |
| 8.2 🟡 | 遲到嚴重度：>15 分=低、>60 分=高。是否合理？ | 15/60 分鐘 | `attendance/views.py:125-216` |
| 8.3 🟡 | 「實際工時超過 12 小時」會自動標記為 high severity。是否合理？ | 12 小時 | `attendance/views.py:142` |
| 8.4 🟡 | 異常記錄（AnomalyRecord）只能**人工解除**（resolved=True）。是否需要「自動解除規則」（例如有對應請假單就自動解）？ | 僅人工 | `attendance/models.py:75-127` |
| 8.5 🟡 | 是否需要「請假管理」？目前**完全沒有 LeaveRequest 模型**。如果有人請假，目前要直接改 EmployeeAvailability 或 Schedule。 | 無 | — |
| 8.6 🔴 | 打卡是用什麼介面？手機 App、實體打卡機、人臉辨識、其他？目前 API 只接收 `clock_in` / `clock_out` 兩個 endpoint，誰來叫不限。 | API 不限制 | `attendance/views.py` |
| 8.7 🟡 | 打卡時是否需要 GPS 位置驗證？目前**模型有 `location` 欄位但不驗證**。 | 不驗證 | `attendance/models.py` |

---

## 9. 合規檢查行為

| # | 問題 | 目前預設 | 來源 |
|---|------|---------|------|
| 9.1 🔴 | 合規檢查（ComplianceCheck）目前是「**手動觸發**」（呼叫 API），不會「**班表發布時自動檢查**」。是否需要改成自動？ | 手動 | `compliance/views.py` |
| 9.2 🔴 | 違反合規時：A) 直接擋住發布、B) 警告但允許發布、C) 純記錄不擋。**目前是 C**。 | C | `schedules/views.py:approve` |
| 9.3 🟡 | ComplianceCheck **目前不關聯到 ScheduleVersion**（沒有 FK），只用日期區間查詢。是否需要關聯，方便後續稽核「哪一版排班通過了哪一次合規檢查」？ | 無關聯 | `compliance/models.py` |
| 9.4 🟡 | LaborLawRule 表存在但**未被引擎讀取**——所有規則目前 hard-coded 在 `engine.py`。是否需要打通，讓客戶能編輯規則？ | 未打通 | `compliance/engine.py:18` |

---

## 10. 稽核與資料保留

| # | 問題 | 目前預設 | 來源 |
|---|------|---------|------|
| 10.1 🟢 | AuditLog 是否要設定保留期限？例如自動歸檔 1 年前資料？目前永久保留。 | 永久 | `audit/models.py` |
| 10.2 🟢 | 個資（員工姓名、薪資、Firebase UID）保留多久？是否需要符合 GDPR / 個資法的「被遺忘權」？ | 無自動清除 | — |
| 10.3 🟢 | 排班資料封存後（archived）是否仍可被查詢？目前可以。 | 可查 | — |

---

## 11. 前端整合與 UX

| # | 問題 | 目前預設 | 來源 |
|---|------|---------|------|
| 11.1 🔴 | 前端使用什麼框架？React + TypeScript、Vue、Angular？目前文件假設 **React + TypeScript + openapi-typescript-codegen**。 | React + TS | `docs/FRONTEND_DEVELOPER_GUIDE.md` |
| 11.2 🔴 | UI 語言：純中文、中英雙語、多國語言？目前所有 verbose_name、choices display 都是繁體中文。 | 繁中 | 全專案 |
| 11.3 🔴 | 排班 UI 風格：A) 月曆視圖（每格一格子） B) 甘特圖（時間軸） C) 表格（員工 × 日期） D) 三者都要？ | 待客戶決定 | — |
| 11.4 🟡 | 是否需要拖拉換班 UI？如果需要，要用 `evaluate_change` 端點先評估影響再決定是否套用嗎？ | 待客戶決定 | `ai_engine/views.py:332-381` |
| 11.5 🟡 | 版本比較（`compare`）UI：是否要做 side-by-side diff、紅綠標記、合併工具？ | 待客戶決定 | `schedules/views.py:106-153` |
| 11.6 🟡 | 是否需要「離線使用 / PWA」？目前 API 無離線支援設計。 | 無 | — |
| 11.7 🟡 | Mobile（手機 App）是否要做？還是純 Web 響應式？ | 待客戶決定 | — |
| 11.8 🟡 | 通知系統：排班發布、被指派加班、合規違反，要不要 push notification、email、簡訊？目前**完全沒實作**。 | 無 | — |
| 11.9 🟢 | 前端列印 / 匯出：班表 PDF、Excel、CSV？目前**無實作**。 | 無 | — |

---

## 12. 部署、認證與整合

| # | 問題 | 目前預設 | 來源 |
|---|------|---------|------|
| 12.1 🔴 | 是否真的要用 Firebase Auth？目前**啟動時若無 Firebase 憑證直接 raise**——必須提供。 | 必須 | `accounts/authentication.py:18-42` |
| 12.2 🔴 | 是否需要 SSO（單一登入）？例如 Google Workspace、Microsoft 365、SAML？ | 待客戶決定 | — |
| 12.3 🔴 | 是否需要與既有系統整合？例如：HR 系統、薪資系統、ERP、HIS（醫院資訊系統）？ | 待客戶決定 | — |
| 12.4 🔴 | 部署環境：A) 公有雲（AWS / GCP / Azure） B) 客戶機房 C) 客戶 VPC？ | 待客戶決定 | — |
| 12.5 🔴 | 預估資料規模？目前測試規模約：1 機構 × 5 分店 × 50 員工 × 30 天 × 3 班別。最大會到多少？ | 待客戶提供 | — |
| 12.6 🟡 | 資料庫備份頻率與保留？預設 PostgreSQL 16，無自動備份設定。 | 無 | `docker-compose.yml` |
| 12.7 🟡 | 是否需要符合特定法規（HIPAA、ISO 27001、PCI-DSS）？ | 待客戶決定 | — |
| 12.8 🟡 | 系統時區是否一律 Asia/Taipei (UTC+8)？跨時區營運是否需要支援？ | UTC+8 | `config/settings/base.py:111` |

---

## 13. 尚未實作但可能需要的功能

> 客戶可能以為已有，實際上還沒做的功能。請務必確認哪些是 MVP 必備、哪些可以延後。

| # | 功能 | 目前狀態 | 優先級 |
|---|------|---------|--------|
| 13.1 | **請假管理**（LeaveRequest, leave types, 請假審核流程） | 完全沒做 | ☐ |
| 13.2 | **每日工時上限（8 小時）強制檢查** | 規則定義但未執行 | ☐ |
| 13.3 | **每週至少 1 天休息日強制檢查** | 規則定義但未執行 | ☐ |
| 13.4 | **加班上限自動檢查**（每日/每月） | 規則定義但未執行 | ☐ |
| 13.5 | **加班費自動計算** | OvertimeRecord 只手動填 | ☐ |
| 13.6 | **國定假日表 + 自動套用 holiday/special_holiday 倍率** | 無 | ☐ |
| 13.7 | **通知系統**（push / email / SMS） | 無 | ☐ |
| 13.8 | **報表 / 匯出**（班表 PDF、薪資報表、出勤報表） | 無 | ☐ |
| 13.9 | **客戶可自訂的勞動法規規則編輯介面** | LaborLawRule 模型存在但未被引擎讀取 | ☐ |
| 13.10 | **多語系 i18n** | 全部繁中 hard-coded | ☐ |
| 13.11 | **Employee 跨 Branch 支援能力清單** | 模型無此欄位 | ☐ |
| 13.12 | **EmployeeAvailability 多時段（時間範圍切割）** | OneToOne 限制 | ☐ |
| 13.13 | **班別變體**（同一班別不同時段） | 不支援 | ☐ |
| 13.14 | **拒絕並退回班表審核流程** | 只有單向 approve | ☐ |
| 13.15 | **班表發布時自動觸發合規檢查** | 純手動 | ☐ |
| 13.16 | **GPS / 位置驗證打卡** | 模型有欄位但不驗證 | ☐ |
| 13.17 | **證照到期提醒** | 模型有 expiry_date 但無提醒 | ☐ |
| 13.18 | **薪資（base_salary）連動** | Contract 有欄位但無計算邏輯 | ☐ |

---

## 附錄 A：問題優先順序總表

> 建議按此順序確認：先卡住其他決策的問題優先問。

**🔴 必問（不釐清會做錯）**
- §1 全部（勞動法規）
- §2.1–2.5（組織結構、角色）
- §6 全部（AI 排班規則 — 這是系統靈魂）
- §7.1–7.4（加班政策）
- §8.6（打卡介面）
- §9.1–9.2（合規檢查觸發時機）
- §11.1–11.3（前端框架、語言、排班 UI）
- §12.1–12.5（部署、認證、整合、規模）

**🟡 應問（影響 UX 但可後期改）**
- §3、§4、§5（員工/班別/可用性的細部規則）
- §8.1–8.5、§8.7（出勤異常、請假）
- §9.3–9.4（合規與資料關聯）
- §11.4–11.8（UI 細節、通知）
- §12.6–12.8（備份、時區）
- §13 中標 ☐ 的所有項目

**🟢 可問（可後期決定）**
- §10 全部（保留期限）
- §11.9（匯出功能）
- §13 中其他項目

---

## 附錄 B：開會建議帶的東西

1. 本份問題清單（紙本 + 數位）
2. 後端架構文件（`ARCHITECTURE_BACKEND_FRONTEND.md`）—— 客戶問「你們怎麼做的？」時可秀
3. AI 排班範例輸出（demo 用）
4. 前端 mockup（如果有）
5. 部署規模估算表（待客戶填）

---

> 本文件由系統架構盤點產出，內容對應 commit `f03e1a2` 的程式碼狀態。後續程式碼有更動時請同步更新。
